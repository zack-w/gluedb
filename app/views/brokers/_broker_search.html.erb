<%= form_tag brokers_path, { method: :get, class: "form form-inline"} do %>
  <div class="form-group">
    <%= text_field_tag :q, @q, { placeholder: "Name, NPN", class: "form-control"} %>
    <%= submit_tag "Search", { class: "btn btn-primary" } %>
  </div>
<% end %>
