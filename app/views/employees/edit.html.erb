<div class="page-header">
  <ul class="breadcrumb">
  	<% employer = Employer.find(params[:employer_id]) %>
    <li><%= link_to "Employers", employers_path %></li>
    <li><%= link_to employer.name, employer_path(Employer.find(employer.id)) %></li>
    <li><%= link_to @person.full_name, person_path(@person) %></li>
    <li class="active">Edit</li>
  </ul>
</div>																																								

<%= simple_form_for(@person, url: employer_employees_path, html: { action: :compare, class: 'form-horizontal' }) do |f| %>
	<%= render :partial => 'form', :locals=> {f: f} %>
<% end %>
